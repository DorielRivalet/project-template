const test = require('../eval/fileToEvaluate');

describe('functionName', () => {
  test('describe what the function does', () => {
    const varName = "";
    expect(functionName(varName)).toBe('');
  });
});
